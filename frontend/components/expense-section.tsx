"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, Plus, Receipt, Trash2, X } from "lucide-react";
import { createApi, type Expense } from "@/lib/api/client";
import { createClient } from "@/lib/supabase/client";

async function getApi() { const supabase = createClient(); const session = supabase ? (await supabase.auth.getSession()).data.session : null; return createApi(session?.access_token); }
function money(value: string | number, currency = "USD") { return Number(value || 0).toLocaleString("en-US", { style: "currency", currency }); }

export default function ExpenseSection() {
  const [expenses, setExpenses] = useState<Expense[]>([]);
  const [currency, setCurrency] = useState("USD");
  const [query, setQuery] = useState("");
  const [open, setOpen] = useState(false);
  const [description, setDescription] = useState("");
  const [amount, setAmount] = useState(0);
  const [date, setDate] = useState(() => new Date().toISOString().slice(0, 10));
  const [method, setMethod] = useState("bank_transfer");
  const [notes, setNotes] = useState("");
  const [saving, setSaving] = useState(false);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const visible = useMemo(() => expenses.filter((expense) => expense.description.toLowerCase().includes(query.trim().toLowerCase())), [expenses, query]);

  useEffect(() => { (async () => { try { const api = await getApi(); const [expenseData, workspace] = await Promise.all([api.expenses.list(), api.workspace.me()]); setExpenses(expenseData); setCurrency(workspace.organization?.currency ?? "USD"); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to load expenses"); } finally { setLoading(false); } })(); }, []);

  function openCreate() { setDescription(""); setAmount(0); setDate(new Date().toISOString().slice(0, 10)); setMethod("bank_transfer"); setNotes(""); setError(null); setOpen(true); }
  async function save(event: React.FormEvent) { event.preventDefault(); if (!description.trim() || amount <= 0 || saving) return; setSaving(true); setError(null); try { const api = await getApi(); const created = await api.expenses.create({ description: description.trim(), amount, expense_date: date, payment_method: method, notes: notes.trim() || undefined }); setExpenses((current) => [created, ...current]); setOpen(false); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to add expense"); } finally { setSaving(false); } }
  async function remove(id: string) { if (!window.confirm("Delete this expense?")) return; try { const api = await getApi(); await api.expenses.remove(id); setExpenses((current) => current.filter((item) => item.id !== id)); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to delete expense"); } }

  return <main className="min-h-screen bg-[#f6f8fb] p-5 text-[#17202a] sm:p-8"><div className="mx-auto max-w-6xl"><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-[#2557d6] hover:underline"><ArrowLeft size={16}/> Back to overview</Link><div className="mt-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><div className="flex items-center gap-2 text-sm font-semibold text-[#2557d6]"><Receipt size={17}/>Expenses</div><h1 className="mt-1 text-3xl font-semibold tracking-[-0.04em] sm:text-4xl">Business expenses</h1><p className="mt-2 text-sm leading-6 text-[#687485]">Track costs so your profit numbers stay honest.</p></div><button className="btn-primary" onClick={openCreate}><Plus size={17}/> Add expense</button></div>{error && <div role="alert" className="mt-6 rounded-xl border border-[#f1cccc] bg-[#fff7f7] px-4 py-3 text-sm text-[#a73f3f]">{error}</div>}<div className="card mt-8 p-4"><input className="field max-w-sm" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search expenses"/></div><div className="card mt-4 overflow-hidden">{loading ? <div className="space-y-3 p-5">{[1,2,3].map((item) => <div key={item} className="h-14 animate-pulse rounded-xl bg-[#f3f5f8]"/>)}</div> : visible.length === 0 ? <div className="px-5 py-16 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-[#fff4df] text-[#b97812]"><Receipt size={20}/></div><h2 className="mt-4 font-semibold">No expenses recorded</h2><p className="mt-2 text-sm text-[#8993a2]">Add business costs as they happen so your dashboard stays useful.</p><button className="btn-primary mt-5" onClick={openCreate}>Add expense</button></div> : <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-[#e7ebf0] bg-[#fbfcfe] text-xs uppercase tracking-wider text-[#8993a2]"><tr><th className="px-5 py-4">Description</th><th className="px-5 py-4">Date</th><th className="px-5 py-4">Method</th><th className="px-5 py-4 text-right">Amount</th><th className="px-5 py-4 text-right">Action</th></tr></thead><tbody className="divide-y divide-[#eef1f5]">{visible.map((expense) => <tr key={expense.id} className="table-row"><td className="px-5 py-4"><div className="font-semibold">{expense.description}</div><div className="text-xs text-[#8993a2]">{expense.notes || "Business expense"}</div></td><td className="px-5 py-4 text-[#687485]">{expense.expense_date}</td><td className="px-5 py-4 capitalize text-[#687485]">{expense.payment_method.replaceAll("_", " ")}</td><td className="px-5 py-4 text-right font-semibold">{money(expense.amount, currency)}</td><td className="px-5 py-4 text-right"><button className="rounded-lg p-2.5 text-[#a73f3f] hover:bg-[#fff7f7]" onClick={() => remove(expense.id)} aria-label={`Delete ${expense.description}`}><Trash2 size={15}/></button></td></tr>)}</tbody></table></div>}</div></div>{open && <div className="fixed inset-0 z-50 grid place-items-center bg-[#17202a]/35 p-5 backdrop-blur-sm"><div className="w-full max-w-lg rounded-2xl border border-[#e7ebf0] bg-white p-6 shadow-2xl sm:p-7"><div className="flex items-start justify-between"><div><div className="text-sm font-semibold text-[#2557d6]">New expense</div><h2 className="mt-1 text-2xl font-semibold">Add a business expense</h2></div><button className="rounded-lg p-2 text-[#687485] hover:bg-[#f6f8fb]" onClick={() => setOpen(false)} aria-label="Close"><X size={18}/></button></div><form onSubmit={save} className="mt-6 space-y-4"><label className="block text-sm font-medium">Description<input className="field mt-2" value={description} onChange={(event) => setDescription(event.target.value)} required placeholder="Software subscription"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm font-medium">Amount<input className="field mt-2" type="number" min="0.01" step="0.01" value={amount} onChange={(event) => setAmount(Number(event.target.value))} required/></label><label className="block text-sm font-medium">Date<input className="field mt-2" type="date" value={date} onChange={(event) => setDate(event.target.value)} required/></label></div><label className="block text-sm font-medium">Payment method<select className="field mt-2" value={method} onChange={(event) => setMethod(event.target.value)}><option value="bank_transfer">Bank transfer</option><option value="card">Card</option><option value="cash">Cash</option><option value="mobile_payment">Mobile payment</option><option value="other">Other</option></select></label><label className="block text-sm font-medium">Notes<textarea className="field mt-2 min-h-20" value={notes} onChange={(event) => setNotes(event.target.value)}/></label><div className="flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button type="button" className="btn-secondary" onClick={() => setOpen(false)}>Cancel</button><button className="btn-primary" disabled={saving} type="submit">{saving ? "Saving…" : "Add expense"}</button></div></form></div></div>}</main>;
}
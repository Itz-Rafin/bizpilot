"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, Package, Pencil, Plus, Archive, X, Wrench } from "lucide-react";
import { createApi, type Product, type Service } from "@/lib/api/client";
import { createClient } from "@/lib/supabase/client";

async function getApi() { const supabase = createClient(); const session = supabase ? (await supabase.auth.getSession()).data.session : null; return createApi(session?.access_token); }

type Props = { kind: "products" | "services" };
type Form = { name: string; description: string; sku: string; price: number; cost: number; quantity: number; low: number; duration: number };
const empty: Form = { name: "", description: "", sku: "", price: 0, cost: 0, quantity: 0, low: 0, duration: 60 };

function money(value: string | number, currency = "USD") {
  return Number(value || 0).toLocaleString("en-US", { style: "currency", currency });
}

export default function CatalogSection({ kind }: Props) {
  const productMode = kind === "products";
  const [items, setItems] = useState<Array<Product | Service>>([]);
  const [currency, setCurrency] = useState("USD");
  const [query, setQuery] = useState("");
  const [open, setOpen] = useState(false);
  const [editing, setEditing] = useState<Product | Service | null>(null);
  const [form, setForm] = useState<Form>(empty);
  const [saving, setSaving] = useState(false);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const title = productMode ? "Products" : "Services";
  const Icon = productMode ? Package : Wrench;
  const visible = useMemo(() => items.filter((item) => item.name.toLowerCase().includes(query.trim().toLowerCase())), [items, query]);

  async function load() { try { const api = await getApi(); const [itemsData, workspace] = await Promise.all([productMode ? api.products.list() : api.services.list(), api.workspace.me()]); setItems(itemsData); setCurrency(workspace.organization?.currency ?? "USD"); } catch (cause) { setError(cause instanceof Error ? cause.message : `Unable to load ${title.toLowerCase()}`); } finally { setLoading(false); } }
  useEffect(() => { load(); }, [kind]);

  function openCreate() { setEditing(null); setForm(empty); setError(null); setOpen(true); }
  function openEdit(item: Product | Service) { setEditing(item); setForm({ name: item.name, description: item.description ?? "", sku: "sku" in item ? item.sku ?? "" : "", price: Number(item.price), cost: "cost" in item ? Number(item.cost) : 0, quantity: "quantity" in item ? Number(item.quantity) : 0, low: "low_stock_threshold" in item ? Number(item.low_stock_threshold) : 0, duration: "duration_minutes" in item && item.duration_minutes ? item.duration_minutes : 60 }); setOpen(true); }

  async function submit(event: React.FormEvent) { event.preventDefault(); if (!form.name.trim() || saving) return; setSaving(true); setError(null); try { const api = await getApi(); if (productMode) { const payload = { name: form.name.trim(), description: form.description.trim() || undefined, sku: form.sku.trim() || undefined, price: form.price, cost: form.cost, quantity: form.quantity, low_stock_threshold: form.low }; const updated = editing ? await api.products.update(editing.id, payload) : await api.products.create(payload); setItems((current) => editing ? current.map((item) => item.id === updated.id ? updated : item) : [updated, ...current]); } else { const payload = { name: form.name.trim(), description: form.description.trim() || undefined, price: form.price, duration_minutes: form.duration || undefined }; const updated = editing ? await api.services.update(editing.id, payload) : await api.services.create(payload); setItems((current) => editing ? current.map((item) => item.id === updated.id ? updated : item) : [updated, ...current]); } setOpen(false); } catch (cause) { setError(cause instanceof Error ? cause.message : `Unable to save ${title.toLowerCase()}`); } finally { setSaving(false); } }

  async function archive(item: Product | Service) { try { const api = await getApi(); const updated = productMode ? await api.products.archive(item.id) : await api.services.archive(item.id); setItems((current) => current.map((row) => row.id === updated.id ? updated : row)); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to archive item"); } }

  return <main className="min-h-screen bg-[#f6f8fb] p-5 text-[#17202a] sm:p-8"><div className="mx-auto max-w-6xl"><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-[#2557d6] hover:underline"><ArrowLeft size={16}/> Back to overview</Link><div className="mt-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><div className="flex items-center gap-2 text-sm font-semibold text-[#2557d6]"><Icon size={17}/>{title}</div><h1 className="mt-1 text-3xl font-semibold tracking-[-0.04em] sm:text-4xl">{productMode ? "Your product list" : "Services you provide"}</h1><p className="mt-2 text-sm leading-6 text-[#687485]">Keep pricing and details ready when you create an invoice.</p></div><button className="btn-primary" onClick={openCreate}><Plus size={17}/> Add {productMode ? "product" : "service"}</button></div>{error && <div role="alert" className="mt-6 rounded-xl border border-[#f1cccc] bg-[#fff7f7] px-4 py-3 text-sm text-[#a73f3f]">{error}</div>}<div className="card mt-8 p-4"><input className="field max-w-sm" value={query} onChange={(event) => setQuery(event.target.value)} placeholder={`Search ${title.toLowerCase()}`}/></div><div className="card mt-4 overflow-hidden">{loading ? <div className="space-y-3 p-5">{[1,2,3].map((item) => <div key={item} className="h-14 animate-pulse rounded-xl bg-[#f3f5f8]"/>)}</div> : visible.length === 0 ? <div className="px-5 py-16 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-[#e9efff] text-[#2557d6]"><Icon size={20}/></div><h2 className="mt-4 font-semibold">{query ? `No ${title.toLowerCase()} found` : `Add your first ${productMode ? "product" : "service"}`}</h2><p className="mt-2 text-sm text-[#8993a2]">Keep this list small and useful so invoice creation stays quick.</p>{!query && <button className="btn-primary mt-5" onClick={openCreate}>Add {productMode ? "product" : "service"}</button>}</div> : <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-[#e7ebf0] bg-[#fbfcfe] text-xs uppercase tracking-wider text-[#8993a2]"><tr><th className="px-5 py-4">Name</th><th className="px-5 py-4">Price</th>{productMode && <th className="px-5 py-4">Stock</th>}<th className="px-5 py-4">Status</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody className="divide-y divide-[#eef1f5]">{visible.map((item) => <tr key={item.id} className="table-row"><td className="px-5 py-4"><div className="font-semibold">{item.name}</div><div className="text-xs text-[#8993a2]">{item.description || (productMode && "sku" in item ? item.sku : "")}</div></td><td className="px-5 py-4 font-semibold">{money(item.price, currency)}</td>{productMode && <td className="px-5 py-4 text-[#687485]">{Number((item as Product).quantity).toLocaleString()}</td>}<td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-[11px] font-semibold capitalize ${item.status === "active" ? "bg-[#edf8f4] text-[#1c8c63]" : "bg-[#f1f3f6] text-[#687485]"}`}>{item.status}</span></td><td className="px-5 py-4"><div className="flex justify-end gap-2"><button className="btn-secondary px-3 py-2" onClick={() => openEdit(item)}><Pencil size={14}/> Edit</button>{item.status === "active" && <button className="rounded-lg p-2.5 text-[#a73f3f] hover:bg-[#fff7f7]" onClick={() => archive(item)} aria-label={`Archive ${item.name}`}><Archive size={15}/></button>}</div></td></tr>)}</tbody></table></div>}</div></div>{open && <div className="fixed inset-0 z-50 grid place-items-center bg-[#17202a]/35 p-5 backdrop-blur-sm"><div className="w-full max-w-xl rounded-2xl border border-[#e7ebf0] bg-white p-6 shadow-2xl sm:p-7"><div className="flex items-start justify-between"><div><div className="text-sm font-semibold text-[#2557d6]">{editing ? "Edit" : "New"} {productMode ? "product" : "service"}</div><h2 className="mt-1 text-2xl font-semibold">{editing ? "Update details" : `Add a ${productMode ? "product" : "service"}`}</h2></div><button className="rounded-lg p-2 text-[#687485] hover:bg-[#f6f8fb]" onClick={() => setOpen(false)}><X size={18}/></button></div><form onSubmit={submit} className="mt-6 grid gap-4 sm:grid-cols-2"><label className="block text-sm font-medium sm:col-span-2">Name<input className="field mt-2" value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} required/></label><label className="block text-sm font-medium sm:col-span-2">Description<textarea className="field mt-2 min-h-20" value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })}/></label>{productMode && <label className="block text-sm font-medium">SKU<input className="field mt-2" value={form.sku} onChange={(event) => setForm({ ...form, sku: event.target.value })}/></label>}<label className="block text-sm font-medium">Price<input className="field mt-2" type="number" min="0" step="0.01" value={form.price} onChange={(event) => setForm({ ...form, price: Number(event.target.value) })} required/></label>{productMode ? <><label className="block text-sm font-medium">Cost<input className="field mt-2" type="number" min="0" step="0.01" value={form.cost} onChange={(event) => setForm({ ...form, cost: Number(event.target.value) })}/></label><label className="block text-sm font-medium">Quantity<input className="field mt-2" type="number" min="0" step="0.01" value={form.quantity} onChange={(event) => setForm({ ...form, quantity: Number(event.target.value) })}/></label><label className="block text-sm font-medium">Low stock alert<input className="field mt-2" type="number" min="0" step="0.01" value={form.low} onChange={(event) => setForm({ ...form, low: Number(event.target.value) })}/></label></> : <label className="block text-sm font-medium">Duration (minutes)<input className="field mt-2" type="number" min="1" value={form.duration} onChange={(event) => setForm({ ...form, duration: Number(event.target.value) })}/></label>}<div className="flex flex-col-reverse gap-3 pt-2 sm:col-span-2 sm:flex-row sm:justify-end"><button type="button" className="btn-secondary" onClick={() => setOpen(false)}>Cancel</button><button className="btn-primary" disabled={saving} type="submit">{saving ? "Saving…" : editing ? "Save changes" : `Add ${productMode ? "product" : "service"}`}</button></div></form></div></div>}</main>;
}